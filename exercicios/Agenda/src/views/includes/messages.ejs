<% if (errors.length > 0) {%>
  <div class="alertError">
    <% errors.forEach(erro => {%>
      <%= erro%><br>
    <%});%>
  </div>
<%}%>

<% if (success.length > 0) {%>
  <div class="alertSucess">
    <% success.forEach(success => {%>
    <%= success %><br>
    <%});%>
  </div>
<%}%>




