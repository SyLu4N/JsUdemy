const nome = document.getElementsByName('nome');
const sobrenome = document.getElementsByName('sobrenome');
const peso = document.getElementsByName('peso');
const altura = document.getElementsByName('altura');

console.log(nome);

document.div.innerHTML(nome);

